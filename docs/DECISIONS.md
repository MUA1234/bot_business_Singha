# DECISIONS.md

Decision log. When the spec is silent or two inputs conflict, the resolution is
recorded here with the **safer** reading preferred (per the build prompt).

| ID | Decision | Rationale | Status |
|---|---|---|---|
| D-001 | Build the new platform in `bot_business_Singha`; treat the Sasiri bot (`Automation_Singha_Auctions`) as an existing system to reuse patterns from and integrate later, not to re-platform. | Sasiri is a customer sales bot with minimal domain overlap; re-platforming it risks its live traffic. Preserves existing bot behaviour (constraint #6). | Adopted |
| D-002 | Use Inngest for all money-touching, long-running and scheduled work; keep simple request/response in API routes. | Vercel Cron gives no exactly-once, durable retry/dead-letter, or long jobs; a doubled QuickBooks post costs real money (build-prompt §2). Free tier. | Adopted |
| D-003 | `company_id` on every table references the operating `businesses` row; the org hierarchy (legal_entity/branch/department) is modelled separately; cross-company reporting via authorised views only. | Makes isolation a single, testable key while still supporting the full hierarchy (spec §6). Safer than multi-key RLS in the pilot. | Adopted |
| D-004 | Test runner = Vitest. | TS-native, fast, no extra infra/paid service (cost rule). | Adopted |
| D-005 | Order Authority (7) before WhatsApp (8), reconciling the build-prompt order with spec §29. | WhatsApp-driven actions must route through the authority engine, so it must exist first. Spec wins where it conflicts; here both are compatible. | Adopted |
| D-006 | No model IDs outside the AI gateway routing table; enforced by a grep test. | Provider independence (spec §20); removes existing-bot inline-model-ID debt. | Adopted |
| D-007 | Webhook signature mismatch is a **hard reject**, not a warning. | Fixes existing-bot risk R-3; security §23. Safer reading. | Adopted |
| D-008 | GPS/CCTV/agents/multi-country are not even schema-migrated until their gate clears. | Privacy gate (constraint #7); avoids dormant sensitive tables. | Adopted |
| D-009 | Money movement (bank payment execution) is entirely out of pilot scope; pilot only records purpose + prepares QuickBooks drafts. | Human-in-the-loop (constraint #2, spec §13/§16). | Adopted |

## Open decisions (need management/finance input)

See `OPEN_QUESTIONS.md`. These will become D-0xx entries once answered:
pilot entity/business identity; staff roster & authority thresholds; QuickBooks
entity + sandbox availability; WhatsApp number strategy; retention appetite; approval
owners for finance and (later) privacy/legal.

## How to add a decision

Append a row with the next `D-0xx` id, the decision, the rationale (why the safer
reading), and status. If it reverses an earlier decision, mark the old one
`Superseded by D-0xx`.

| D-010 | The new platform is a fully standalone fresh build. It must NOT reuse the Sasiri auction bot's (`Automation_Singha_Auctions`) APIs, WhatsApp number, credentials, database, or code. The Sasiri bot is a pattern reference only. This platform provisions its own WhatsApp number, Supabase project, OpenAI gateway, and all other services. | User instruction 2026-07-27: this should be a new fresh thing, not built on the existing auction bot's APIs. Avoids coupling live sales traffic and keeps clean isolation. | Adopted |
| D-011 | **Remove QuickBooks entirely.** Build an internally-owned double-entry **Accounting Core v0** as the accounting source of truth, per `Claude_Interim_Accounting_Development_Guide.md`. This supersedes D-002/D-009's QuickBooks-as-truth stance and Phase 11 of the phased plan. | User instruction 2026-07-30: QuickBooks is too pricey; we own the financial system. The guide's executive decision is to replace the planned QuickBooks dependency. | Adopted — supersedes QuickBooks-as-truth in D-002/D-009 |
| D-012 | Invoices/statements are generated from **our own HTML templates** (self-contained, no external assets) and rendered to PDF for free (headless browser / "Save as PDF"); ledgers/logs export to **Excel-compatible CSV** (UTF-8 BOM). No paid invoicing/PDF service. Google Sheets/Excel remain a view + import/export surface, never the ledger. | User instruction 2026-07-30: "add details to a template created by our own… save logs in excel and use a generated pdf template… make this work for free." Honours the cost rule. | Adopted |
| D-013 | Add `decimal.js` (fixed-precision money — guide invariant #11) and `@supabase/supabase-js`, `inngest`, `zod`, `next` as production deps. All free/OSS; none is a paid managed service. Money is never a JS `number`. | Guide bans floating-point money; the mandated stack needs these clients. Cost rule satisfied (all free tiers/OSS). | Adopted |
| D-015 | **AI model = `gpt-5.6-sol` for all routes, via the OpenAI Responses API** (`/v1/responses`), not Chat Completions. The transport maps system→`instructions`, user→`input`, maxTokens→`max_output_tokens`, and uses `text.format=json_object` (which requires the word "json" in the input, so the transport appends a JSON directive). Model ids stay confined to the gateway routing table (D-006). | User instruction 2026-08-02: "use gpt-5.6-sol only." The key's project has the gpt-5.x family but **not** gpt-4o/4o-mini; and `gpt-5.6-sol` is served on the Responses API (Chat Completions 403s for it). **Caveat:** this project's access to `gpt-5.6-sol` was observed to be intermittent (1 of 3 calls 200, the rest 403 `model_not_found`) — an OpenAI account/project-side issue (enable model access / org verification / tier), not a code issue. A 403 is treated as a retryable `transport_error`, so Inngest's retries partially absorb the flakiness. | Adopted — resolve model access on the OpenAI side before go-live |
| D-016 | **Operational app layer: employee auth + department dashboards + admin panel.** Add `@supabase/ssr` (free/OSS) for App-Router cookie sessions. Employees log in with a **username + password**; the username is mapped to a synthetic email `<username>@singha.local` so Supabase Auth (email-based) still works, while staff never see an email field. A new `profiles` table (migration `0007`) links `auth.users` → a `department` + platform `role`; on login a user is redirected to their department dashboard. Admins create/deactivate employees and assign departments via server actions using the service-role `auth.admin` API (never in the browser). Departments are a seeded-but-extensible catalog (admin, finance, sales, marketing, operations, hr, procurement). | User instruction 2026-08-04: "admin panel to add any employee type, create username+password, pick department; each logs into their own dashboard; separate the database so dashboards don't conflict." Username→synthetic-email keeps the mandated Supabase Auth without a custom password store (safer). RLS + department scoping is the isolation boundary. | Adopted |
| D-017 | **Fully-AI WhatsApp quotation with a deterministic price-confirmation fallback.** A customer message drives an AI conversation that collects customer details (name, address, phone, requested items) and drafts a quotation. When a **price is missing or the model is not confident**, the quotation is **not** sent; instead a `price_confirmation` row is routed to the relevant department dashboard. While it is pending, the AI keeps answering the customer, and **every outbound message during that window ends with the exact footer line `(Quotation is being generated. Please wait)`**. Once staff fill the price, the quotation is finalized and sent. No free-text AI output ever sets a price that goes to a customer without either a catalog price or human confirmation (AI-safety + financial controls). | User instruction 2026-08-04. Keeps the customer experience automated while ensuring a human validates any uncertain price before it leaves the company. | Adopted |
| D-018 | **UI icons use `lucide-react`** (MIT, tree-shakeable SVG), not emojis. Department/nav icons are stored as stable string names in `src/lib/departments.ts` and resolved by `src/components/Icon.tsx`. | User instruction 2026-08-04: emojis look unprofessional and render inconsistently across devices; use a well-known icon library. Lucide is the widely-used set behind shadcn/ui; SVG (not a webfont) keeps it self-contained and light. Free/OSS — cost rule satisfied. | Adopted |
| D-019 | **WP1 identity/RLS approach (2026-08-06).** Make the **membership** model (0010) the access source of truth via migration `0023`: `has_company_access` is **extended** (union of legacy `user_company_access` OR active membership) so no cutover locks anyone out; new `has_membership` + `has_capability(company, permission)` functions replace department-name checks; `role_permissions` seeded for the membership role vocabulary; capability/ownership **write** RLS policies added (inert while the app uses service role); composite `(id, company_id)` FKs added for `tasks`/`task_dependencies`/`task_assignments`; delegations gain an authority ceiling. Central helpers `src/lib/access.ts` (`requireMembership`/`requireCapability`/`requireCompanyRecord`) read the RLS client; pure `canActOnTask` (`src/modules/identity/`) encodes the §WP3 permission + separation-of-duties rules (8 unit tests). The service-role→RLS cutover of the ~120 files is deferred to later staged, owner-approved slices. Live isolation tests (`tests/integration/company-isolation.test.ts`) require a real Postgres and are **gated on `DATABASE_URL`**, refusing to run against the prod project; running them needs `npm i -D pg` and a throwaway DB (no dep added to `package.json`/lockfile now — keeps `npm ci` clean; cost rule satisfied). | NEXT_PHASE_DEVELOPER_BRIEF §WP1. Union-extension + additive migration = zero-lockout, fully reversible; no prod schema/auth change is applied by building this slice. Mocked unit tests cannot prove RLS, so live tests are provided but must run on a non-prod DB (none available in this environment). | Adopted — slice 1 (foundation); cutover + live verification pending owner-provided test DB |
| D-020 | **WP3 staff progress + WP5 AI cost (2026-08-06).** WP3: additive migration `0025` (task actual/remaining/blocker/ETA + assignment estimate + capacity fields); pure `task-progress.ts` workflow engine + rich `capacity-detail.ts` (planned/actual/remaining/free/blocked/overdue, reproducible from records); worker/manager server actions authorised via `canActOnTask` (any-dept assignee), company-scoped + audited + graceful pre-0025; task-detail Progress UI + rich `/app/hr/capacity`. WP5: model prices moved to `src/ai/pricing.ts`; **`gpt-5.6-sol` price is env-configured** (`OPENAI_PRICE_GPT56_INPUT_PER_MTOK` / `OPENAI_PRICE_GPT56_OUTPUT_PER_MTOK`, USD per 1M tokens) so recorded cost stops being 0 — no rate is guessed; unknown/unpriced model still records an explicit "0". | NEXT_PHASE_DEVELOPER_BRIEF §WP3/§WP5.4. Pure engines are fully unit-tested without a DB; env-configured pricing keeps the real rate an owner decision (D-015 notes gpt-5.6-sol access is account-side). | Adopted — set the two price env vars in Vercel to activate non-zero AI cost |
| D-014 | **Consumer pipeline wired past the webhook boundary** (2026-08-02): `src/inngest/processing.ts` runs extract→detect-missing→dedup→draft→policy→approval/clarification→audit as pure orchestration over ports; the live Inngest function binds Supabase + the gateway. Approval policy is stored per company as validated JSONB in a new `approval_policies` table (migration `0006`). The live OpenAI transport (`src/ai/openai-transport.ts`) uses the global `fetch` — **no OpenAI SDK dependency added** (cost/dependency rule). Model ids stay confined to the gateway routing table + this transport. | User instruction 2026-08-02: "decide on your own so you can fully complete it." The pure logic was already built + tested; wiring it end-to-end completes the phase without needing live keys (keys only *activate* it). `fetch` avoids a new dependency; a policy table makes deterministic auto-approve reachable in production, not just tests. Auto-approve still never posts a journal or moves money (financial controls). | Adopted |
| D-021 | **Railway is a supported deployment target for the Next.js app, alongside Vercel** (2026-09-01). A dedicated Railway **project** `singha-central` (id `cf0cfa34-bdc5-4e10-8753-f8d89907cf65`) holds ONE service, `singha-web`, deployed from GitHub `MUA1234/bot_business_Singha`@`main` at `https://singha-web-production.up.railway.app`. It runs the same `next build` / `next start` and talks to the same Supabase project; no code, schema, or app behaviour changed. The owner's three pre-existing Railway projects (`Important`/core-api, `nurturing-encouragement`/GSI-Certification-System, `charming-commitment`/Auctions-Backend) were **not** touched — a separate project was chosen over adding a service to one of theirs precisely to make interference impossible (Railway bills per account, so a separate project costs nothing extra). `CRON_SECRET` was generated fresh for this environment rather than copied from Vercel, so the two deployments do not share a cron credential. `INNGEST_EVENT_KEY`/`INNGEST_SIGNING_KEY` are intentionally unset — they are blank locally and unused while `WHATSAPP_ASYNC` is OFF. | Owner instruction 2026-09-01: "deploy everything in the railway so it will have a proper server… create a new service… this project should properly use railway." This is a **deviation from the mandated stack** in CLAUDE.md ("Frontend + API: Next.js on Vercel") and from the COST RULE ("every service must have a usable free tier") — Railway has no free tier, only a paid Hobby plan; the owner already runs three paid services there, and the owner's explicit instruction is the highest precedence under the CLAUDE.md conflict rule. The operational gain is real: Railway runs a long-lived server rather than serverless functions, which removes the 25s middleware timeout class seen on Vercel and makes a genuine scheduler possible for the outbox drain (Vercel Hobby caps crons at 2/day). **Meta's WhatsApp webhook can point at only ONE origin** — whichever host is not receiving it processes no inbound messages. | Adopted — Railway live and health-green; webhook still pointed at Vercel pending owner repoint |
| D-022 | **The membership model is written by the admin panel, and price-confirmation routing may only address a department that can resolve one** (2026-09-11). (a) Creating an employee now provisions `users` + `memberships` + `membership_roles` alongside the `profiles` row (`src/lib/identity-provisioning.ts`), fails CLOSED (rolling the whole creation back) if that provisioning fails, and mirrors activate/deactivate onto `memberships.status`; migration **0070** repairs the five live accounts that had a profile only. This is step 5 ("point admin writes at memberships") of `IDENTITY_UNIFICATION_PLAN.md`, brought forward because the drift was already live and silent. (b) The admin DEPARTMENT no longer implies admin RIGHTS anywhere: one resolver, `landingPathFor`, decides where an employee lands, and creating a non-admin in the `admin` department is refused. (c) `PRICE_CONFIRM_DEPARTMENTS` is now the single source of truth shared by routing, the pricer authorisation and the pages — routing validated against the nine-department catalogue alone, but only `sales`/`finance` render a queue or may price, so a confirmation routed elsewhere was unresolvable. | Found on the live system 2026-09-11. Each was silent: the five staff could sign in, so nothing looked wrong until RLS became the enforcement path; four staff in the admin department hit an INFINITE redirect and could not use the app at all; both live catalogue products name `procurement`, so the next human-priced item would have produced a confirmation nobody could act on while the customer waited behind the pending footer. Fail-closed + one resolver per decision is the safer reading in every case. | Adopted — code merged to `main`; migration 0070 awaiting owner authorisation for the hosted DB |
| D-023 | **Inbound email is a real ingestion channel, routed by the delivery address** (2026-09-12). `/api/webhooks/email` stops being a stub: HMAC-SHA256 over the RAW body against `EMAIL_WEBHOOK_SECRET` (header name configurable via `EMAIL_WEBHOOK_SIGNATURE_HEADER`, default `x-signature-256`), then persist, then enqueue — the WhatsApp route's order. The company is resolved from `companies.inbound_email_address` (migration **0071**: nullable column, lower-case CHECK, partial UNIQUE index), never guessed. Three fail-closed rules: an UNSET secret refuses every request (503) rather than accepting unauthenticated mail; a bad signature is a hard 401; and mail to an address no company claims is **persisted with a NULL company and marked `failed`**, not dropped — visible in `/api/health`, replayable once the address is mapped. The consumer (`financial/source_event.received`) now closes the source event's lifecycle, which it never did because until now nothing produced that event. Extraction reads the email as subject + body rather than the provider envelope. | `STATUS.md` open item 6 / `singha-open-owner-items` item 5 — the last documented stub route in the app. HMAC-SHA256-over-raw-body is the common denominator across inbound-parse providers, so the choice of provider stays the owner's without a code change; the ALGORITHM is fixed rather than provider-declared, because accepting whatever the caller names is how signature checks get bypassed (D-007 applies to every webhook, not just Meta's). Routing on the delivered-to address is the exact analogue of `companies.whatsapp_phone_number_id` (0069) and keeps company attribution derived from the event — guessing it is the cross-company leakage CLAUDE.md calls critical. Persisting unroutable mail follows 0070's rationale: an unattributable event must become VISIBLE, because it is durable evidence and a configuration gap is not a reason to lose it. | Adopted — code complete and tested; INERT until the owner sets `EMAIL_WEBHOOK_SECRET` and an `inbound_email_address`, and 0071 is applied to the hosted DB |
